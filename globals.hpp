#ifndef GLOBALS
#define GLOBALS
#include <string>

enum {
	ERR_ARGUMENT = 1,
	ERR_SOCKET,
	ERR_SEND,
	ERR_RECIEVE,
	ERR_BIND,
	ERR_LISTEN,
	ERR_FILE
};

enum {
	STATE_START = 0,
	STATE_USER,
	STATE_PASS,
	STATE_AUTHORIZED,
	STATE_QUIT
};
#endif
